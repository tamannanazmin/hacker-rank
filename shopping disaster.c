#include<stdio.h>

int main()
{
    int n,a,sum=0;
    scanf("%d",&n);
    while(n--)
        {

        scanf("%d",&a);
        sum = sum+a;

        }
        printf("Total = %d\n",sum);
        return 0;
}
